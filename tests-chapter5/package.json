{
  "name": "e2e-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report",
    "start:backend": "MONGOMS_VERSION=7.0.14 MONGOMS_DOWNLOAD_DIR=/tmp/mongodb-binaries node setup/start-test-backend.js",
    "start:frontend": "npm run dev --prefix ../library-frontend"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "baseline-browser-mapping": "^2.10.20",
    "mongodb-memory-server": "^10.4.0"
  }
}
